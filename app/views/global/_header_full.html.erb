<nav id="headerNavFull" class="navbar navbar-expand-lg navbar-light sticky-top mb-3 mb-lg-4">
  <div class="container-fluid">
    <a class="navbar-brand fw-bolder" href="/"><i class="fa-solid fa-seedling me-2"></i>Seattle Plant Journal</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarCollapse">
      <ul class="navbar-nav small me-auto mb-2 mb-md-0">
        <li class="nav-item">
          <a class="nav-link" href="/creatures">Plant List</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">How To Use</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">About Us</a>
        </li>
        <% if current_user&.admin? %>
          <li class="nav-item">
            <a class="nav-link text-spj-primary" href="/admin/home"><i class="fa-solid fa-lock me-2"></i>Admin Home</a>
          </li>
        <% end %>
      </ul>
      <div class="d-flex gap-2">
        <% if user_signed_in? %>
          <span class="navbar-text small">
            Welcome, <%= current_user.email %>
            <% if current_user.admin? %>
              <span class="badge bg-primary">Admin</span>
            <% end %>
          </span>
          <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "btn btn-outline-secondary btn-sm" %>
        <% else %>
          <%= link_to "Login", new_user_session_path, class: "btn btn-outline-secondary btn-sm me-2" %>
          <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-outline-secondary btn-sm me-2" %>
        <% end %>
        <a href="https://google.com" class="btn btn-spj-primary d-flex">
          <i class="fa-solid fa-book me-2"></i>Get Our Journal!
        </a>
      </div>
    </div>
  </div>
</nav>
