<p style="color: green"><%= notice %></p>

<% content_for :title, "Resource Tags" %>

<h1>Plant Resource Tags</h1>

<%= link_to "New Tag", new_tag_path, class: "btn btn-spj-primary mb-3" %>

<div id="tags">
  <% @tags.each do |tag| %>
    <%= render tag %>
  <% end %>
</div>
